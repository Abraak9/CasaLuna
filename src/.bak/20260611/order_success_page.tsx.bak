import Link from 'next/link';

export default function OrderSuccessPage() {
  return (
    <main className="min-h-screen bg-gray-50 flex flex-col items-center justify-center px-6 text-center">
      <div className="bg-white rounded-3xl p-8 max-w-sm w-full shadow-sm border border-gray-100">
        <div className="w-16 h-16 cl-gradient rounded-full flex items-center justify-center mx-auto mb-4 text-2xl">
          ✓
        </div>
        <h1 className="text-2xl font-bold mb-2">Booking Confirmed!</h1>
        <p className="text-gray-500 text-sm leading-relaxed mb-6">
          Your tickets are on their way. Check your email for the QR codes — you'll need them at the door.
        </p>
        <div className="bg-amber-50 rounded-xl p-3 text-xs text-amber-700 mb-6">
          💡 Didn't receive the email? Check your spam folder.
        </div>
        <Link href="/" className="block w-full cl-gradient text-white font-bold py-3.5 rounded-2xl text-sm">
          Back to Events
        </Link>
      </div>
    </main>
  );
}
